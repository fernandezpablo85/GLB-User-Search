#Search GLB Users tool

## Usage

install rubygems:
<code> brew install rubygems </code>

install json gem:
<code> gem install json </code>

make script executable:

chmod +x ./find_user

search users by name:

./find_user pablo
